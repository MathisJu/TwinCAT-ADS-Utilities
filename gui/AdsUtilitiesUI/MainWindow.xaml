<Window x:Class="AdsUtilitiesUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:local="clr-namespace:AdsUtilitiesUI"
        d:DataContext="{d:DesignInstance local:MainWindow, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <!--Route Settings-->
            <RowDefinition Height="35" />
            <!--Main Area-->
            <RowDefinition Height="2*"/>
            <!--Divider-->
            <RowDefinition Height="auto" />
            <!--Logs Area-->
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <GridSplitter Grid.Row="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="LightGray" ShowsPreview="True" Height="1" />

        <!--Routes Settings-->
        <Grid>
            <Grid.ColumnDefinitions>
                <!--Route Selection-->
                <ColumnDefinition Width="1*" />
                <!--Add Route-->
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <ComboBox Name="CmbBx_SelectRoute" 
                      ItemsSource="{Binding StaticRoutes}" 
                      DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedRoute}" 
                      Margin="2 5" 
                      SelectionChanged="CmbBx_SelectRoute_SelectionChanged"></ComboBox>
            <Button Name="Btn_AddRoute" Content="Add Route" ToolTip="Add Route" Grid.Column="1" Margin="5"/>
        </Grid>

        <TabControl Grid.Row="1" Margin="5,5,5,5" Grid.RowSpan="3">
            <TabItem Header="Device">
                <TextBox Name="TxtBx_DeviceInfo" Margin="2" IsReadOnly="True" />
            </TabItem>
            <TabItem Header="Operating System">
                <TextBox Name="TxtBx_OsInfo" Margin="2" IsReadOnly="True" />
            </TabItem>
            <TabItem Header="Installed Functions">
                <TextBox Name="TxtBx_TfInfo" Margin="2" IsReadOnly="True" />
            </TabItem>
            <TabItem Header="File Handling">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <GroupBox Grid.Column="0" Header="Local" Margin="2">

                    </GroupBox>
                    <GroupBox Grid.Column="1" Header="Target" Margin="2">

                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>

        <!--Logs Box-->
        <GroupBox Grid.Row="5" Header="Logs" Margin="5,5,5,5">
            <TextBox Name="TxtBx_Logs" Margin="2" IsReadOnly="True" />
        </GroupBox>
    </Grid>
</Window>
